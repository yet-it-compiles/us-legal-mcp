services:
  - type: web
    name: us-legal-mcp
    runtime: node
    buildCommand: npm install --ignore-scripts && npm run build
    startCommand: node dist/http.js
    envVars:
      - key: NODE_ENV
        value: production
